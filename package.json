{
  "name": "quizzy",
  "version": "0.1",
  "description": "Practising Frontend ,Quiz Project, refrence:CJ'",
  "main": "index.html",
  "scripts": {
    "start": "npm run lite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite": "lite-server",
    "clean": "rimraf dist",
    "imagemin": "imagemin images/* -o dist/images",
    "usemin": "usemin index.html -d dist --htmlmin -o dist/index.html",
    "build": "npm run clean && npm run imagemin && npm run usemin"
  },
  "author": "Naren Raj,credits:Cj",
  "license": "ISC",
  "devDependencies": {
    "cssmin": "^0.4.3",
    "htmlmin": "0.0.7",
    "lite-server": "^2.4.0",
    "rimraf": "^2.6.3",
    "uglifyjs": "^2.4.11",
    "usemin-cli": "^0.5.1"
  }
}